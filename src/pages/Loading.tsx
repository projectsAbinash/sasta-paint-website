import icons from "../assets/icon"
const Loading = ()=>{
    return (
        <div id="loading">
            <img src={icons.preloader} alt="Loading..." />
        </div>
    )
}

export default Loading